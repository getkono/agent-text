# agent-text

Provider-neutral text generation through local AI agent CLIs.

The initial release supports Claude Code. The public API is designed for
additional agent adapters without coupling callers to a particular CLI.

